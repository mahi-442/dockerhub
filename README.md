# Project Explanation
```
CI/CD using Git, Jenkins, Ansible, and Docker.

Developer commits his changes to git, that triggers Jenkins pipeline job. Its going to build using Maven 
and its going to build docker image upload docker image to docker hub using ansible playbook deploy a 
docker container on to development server.

```
